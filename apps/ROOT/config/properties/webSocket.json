{
	"sections": [
		{
			"title": "Members",
			"columns": [
				{
					"name": "Member"
				}, {
					"name": "Type"
				}, {
					"name": "Description"
				}
			],
			"rows": [
				{
					"col1": 'ontext',
					"col2": 'Scriptable',
					"col3": '<pre class="prettyprint">webSocket.ontext = function (data) { \
    log.info(\'Client sent : \' + data); \
 };</pre>Create the instance that will process this inbound connection for text and allow to handle a GET request. '
				},
				{
					"col1": 'onbinary',
					"col2": 'Scriptable',
					"col3": '<pre class="prettyprint">webSocket.onbinary = function (stream) {  \  
    log.info(\'Client Streamed : \' + stream.toString()); \
 };</pre>Create the instance that will process this inbound connection for stream. '
				}
			]
		},
		{
			"title": "Operations",
			"columns": [
				{
					"name": "Operation"
				}, {
					"name": "Returns"
				}, {
					"name": "Description"
				}
			],
			"rows": [
				{
					"col1": 'send (String text | Stream stream)',
					"col2": 'None',
					"col3": '<pre class="prettyprint">webSocket.ontext = function (data) { \
    var ws = this; \
    setTimeout(function () { \
        ws.send(data); \
    }, 5000); \
 };</pre>Sends data to the socket (inbound connection). '
				}
			]
		}
		
	]
}
